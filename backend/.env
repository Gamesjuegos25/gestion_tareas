## Backend-specific environment (optional). Prefer root .env for local runs.
# Uncomment and adjust the line below only if you need per-backend overrides.
# DATABASE_URL="postgresql://user:password@localhost:5432/gestion_tareas?schema=public"
BACKEND_PORT=3001
