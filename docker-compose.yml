version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    # If you want to mount source for development uncomment below
    # volumes:
    #   - ./:/app
